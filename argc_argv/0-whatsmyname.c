#include <stdio.h>
/**
 *main - Entry point
 *@argc: an argument
 *@argv: an argument
 *Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
